[{"title":"Hello World","date":"2026-08-14T14:20:00.000Z","path":"2026/08/14/test/","text":"摘要 第三章BCD码：用二进制表示十进制数的编码方式逻辑移位针对无符号数、算数移位针对有符号数（移的时候不移符号）关于算术左移右移算数左移（* 2）：原码补码无论正负，均为低位补0算数右移（&#x2F; 2）：原码补码先看符号位，正补0 负补1原码补码规则是相同的 ALU的核心部件是加法器补码减法会用&#x3D;&#x3D;进位置0&#x3D;&#x3D;来替代转换补码时的&#x3D;&#x3D;末位加一&#x3D;&#x3D;关于乘法的一些概念快速阵列乘法没有移位器，同时算出所有位的答案然后并行求和。一位乘法（原码一位乘法）指的是每次乘法指处理1个二进制位。相应的：还有两位乘法BOOTH算法（补码一位乘法）：直接用补码完成正负数&#x2F;小数乘法，不用单独处理符号移位方式：原码-逻辑右移 补码-算数右移 设乘数寄存器最低位是Yn​，额外增设 1 位附加位Yn+1​，初始为 0；每次循环判断这两位： 组合 Yn ​Yn+1​ 操作（部分积） 0 0 不加，直接算术右移一位 0 1 加 【X】补，再算术右移一位 1 0 加 【-X】补，再算术右移一位 1 1 不加，直接算术右移一位 每一次整体右移： Q 内部所有位向右挪 1 格； Q 最右侧的最低位 Q0​ 挤出去，变成新的 Yn+1​； A 寄存器移出的最低位，填入 Q 最左侧空位； 1字节（Byte）&#x3D;8bit有符号int的字节数为4，共有4 * 8个二进制位，范围$-2^{31} \\sim 2^{31} - 1$bit代表一个二进制位，寄存器位数、地址位等Byte是编址单位，是KB&#x2F;MB&#x2F;GB的单位 IEEE754单精度浮点数（float）表示格式【1：正负】【8：阶码】【23：尾数】转为二进制后例： $(1.10\\cdots0)_2 \\times 2^{-1}$正负为0阶码为 -1+127 （移码）尾数为10…0阶码的表示范围为-127~128 对应0~255 常见数据类型char：1字节short：2字节int：4字节longlong：8字节float：4字节（1+8+23） 第四章相对寻址的位移量用补码表示MSB：最高有效位 LSB：最低有效位第五章PC中存放的是指令在&#x3D;&#x3D;主存&#x3D;&#x3D;中的地址指令顺序执行后，PC的值增加一条指令的长度（字节&#x2F;字） IR中存放的是当前指令（操作码+地址字段）CPU 分为 数据通路和控制单元数据通路：执行数据操作 状态元件：寄存器、PC、IR 操作元件：ALU、加法器、移位器 控制单元：逻辑电路，输出控制线 CPI：&#x3D;&#x3D;每条指令时钟周期数&#x3D;&#x3D;$T_{\\text{cycle_total}} &#x3D; N \\times \\text{CPI}$ （时钟周期&#x3D;指令数* CPI）$Time &#x3D; \\dfrac{T_{\\text{cycle_total}}}{\\text{主频 } f} &#x3D; \\dfrac{N \\times \\text{CPI}}{f}$ （程序运行时间&#x3D;时间周期&#x2F;主频） 单周期cpu和多周期cpu单周期数据通路一条指令占1个时钟周期（时钟周期由最慢的指令决定&#x2F;CPI&#x3D;1）不需要IR，指令直接在线路上传输 多周期数据通路一条指令占多个时钟周期需要IR硬件利用率高 R型指令：寄存器型指令所有操作数、运算结果全都存放在通用寄存器，不访问内存，均要使用ALUOP(6) | Rs(5) | Rt(5) | Rd(5) | shamt(5) | funct(6)取Rs和Rt做funct运算&#x3D;&#x3D;存入Rd&#x3D;&#x3D;中（OP均为0，shamt是移位量 仅移位指令使用） I型指令：立即数型指令访存、算术立即、逻辑立即、分支lw：load访指令，sw：save存指令，beq：branch_if_equalOP(6) | Rs(5) | Rt(5) | imm16(16)OP指令，Rs基址&#x2F;&#x3D;&#x3D;源操作数&#x3D;&#x3D;1，Rt目标寄存器&#x2F;比较数2，imm16立即数 一些MIPS指令add rd,rs,rt：$rd &#x3D; $rs + $rt，溢出报错addi rt,rs,imm：$rt &#x3D; $rs + 立即数（有符号）lw rt, off($rs)：字加载，从内存地址rs+off读 32 位到 rtbeq rs, rt, label：若 $rs &#x3D;&#x3D; $rt，跳转到 labelbne rs, rt, label：若 $rs !&#x3D; $rt，跳转到 labelsll rd,rt,shamt 逻辑左移：$rd &#x3D; rt &lt;&lt; shamt（补 0） 第六章采用流水线方式会延长一条指令的执行时间，但总的时间会节约加倍的增加流水线个数 不能 成倍提高指令执行效率（实际吞吐率受冲突限制） 组合逻辑部件：ALU、加法器、译码电路。时序逻辑部件：流水寄存器数据冒险：后面指令用到的数据还未来得及由前面数据产生采用 转发（旁路） 技术可以解决部分控制冒险 控制冒险：转移指令或异常等情况改变了程序处理的流程，使已经被取到流水线中的指令无效采用分支预测技术 可用于控制冒险处理 关于一点单位$1ps&#x3D;10^{3}nm&#x3D;10^{6}μs&#x3D;10^{9}ms&#x3D;10^{12}s$ 第七章在计算机底层的内存容量换算方法：$1GB&#x3D;2^{10}MB&#x3D;2^{20}KB&#x3D;2^{30}B$ 我应该认识你们吧RAM：随机访问存储器 SRAM：静态ram 速度快 用作cache 成本高 DRAM：动态RAM 读后需要再生 动态刷新 &#x3D;&#x3D;行列地址复用&#x3D;&#x3D;（RAS行地址选通 CAS列地址选通）ROM：只读存储器EPROM：可擦除可编程 只读存储器CAM：相联存储器（按照内容进行寻址访问） RAID将多块物理硬盘组合为一个逻辑硬盘阵列目标：提升读写性能、数据冗余容错、扩容 CRC校验（循环冗余校验） 将生成多项式转二进制，位数为r+1 在原数据后面补 r 个0，除以 生成多项式的二进制数（模2除法&#x2F;异或）。 把余数添加到原数据后面，发送 校验时将得到的数除以 生成多项式的二进制数 看余数是否为0 地址转换地址转换是将逻辑地址转换为物理地址逻辑地址的位数比物理地址更多目的：- 隔离进程地址空间；实现内存离散分配、虚拟内存（换入换出）；内存共享、地址保护 若Page未命中 Cache也一定未命中（TLB是否命中不构成矛盾。TLB：快表（转换后援缓冲器）。内置于&#x3D;&#x3D;CPU中&#x3D;&#x3D; 缓存近期使用的{页号-页框号&#x2F;虚拟地址和物理内存}映射） 每一个进程都有一个页表。进程被划分为等长的页、内存被划分为相同大小的页框段式存储管理程序按逻辑模块划分为段（代码段、数据段、栈段，长度不固定）；每个段拥有独立段号、段长度。段式存储管理相比于分页式更利于存储保护 看不懂计算主存地址总位数 &#x3D; Tag 位 + 组索引位 + 块内偏移位 块内偏移：log2​(块字节数) 组索引：log2​(组数)&#x3D;log2​(总块数&#x2F;相联路数) Tag：剩余高位Cache 总容量 &#x3D; 数据存储位 + Tag+control位写直达control为1，写回control为2","updated":"2026-09-07T06:01:44.320Z","excerpt":"摘要","categories":[{"name":"Diary","slug":"Diary","permalink":"http://example.com/categories/Diary/"}],"tags":[{"name":"test","slug":"test","permalink":"http://example.com/tags/test/"}]},{"title":"一个很乱的板子","date":"2026-08-14T06:14:00.000Z","path":"2026/08/14/算法模板/","text":"一个废物的算法竞赛模板，没有目录，只有乱乱的内容 永远记不住的ta们可以直接用 &#x3D;（赋值运算符） 进行深拷贝。 vector 动态数组，支持随机访问。 尾部插入 &#x2F; 删除高效（均摊 O (1)），中间插入 &#x2F; 删除较慢（O (n)）。 内存连续，可能需要重新分配内存（导致元素复制） 123456789101112131415161718192021222324252627282930vector&lt;int&gt; v = &#123;1, 2, 3&#125;;vector&lt;vector&lt;int&gt;&gt; a(N, vector&lt;int&gt;(N)); //（二维）// 容量v.size(); // 返回元素个数v.capacity(); // 返回当前分配的容量v.reserve(10);// 预分配至少10个元素的空间v.resize(5); // 调整大小为5个元素v.empty(); // 判断是否为空// 访问v[2]; // 随机访问（不检查边界）v.at(2); // 随机访问（检查边界，越界抛出异常）v.front(); // 返回第一个元素v.back(); // 返回最后一个元素// 修改v.push_back(4); // 尾部插入v.pop_back(); // 尾部删除v.insert(v.begin()+1, 5); // 在第二个位置插入5v.erase(v.begin()); // 删除第一个元素v.clear(); // 清空所有元素//可以使用sort：sort(v.begin(),v.end());vector&lt;int&gt; a = &#123;1, 2, 3&#125;;vector&lt;int&gt; b;b = a; // 深拷贝赋值：b 成为 a 的独立副本 deque 双端队列，支持头尾高效插入 &#x2F; 删除（O (1)）。 随机访问效率略低于vector。 内部由多个固定大小的数组组成，避免了vector的内存重分配问题。 12345678910std::deque&lt;int&gt; dq = &#123;1, 2, 3&#125;;// 相比vector新增的函数dq.push_front(0); // 头部插入dq.pop_front(); // 头部删除// 其他函数与vector类似dq.size();dq[1];dq.insert(dq.begin()+1, 4); list 双向链表，任意位置插入 &#x2F; 删除高效（O (1)）。 不支持随机访问（只能顺序遍历）。 内存不连续，每个元素包含前后指针。 1234567891011121314151617std::list&lt;int&gt; lst = &#123;1, 2, 3&#125;;// 相比vector新增的函数lst.push_front(0); // 头部插入lst.pop_front(); // 头部删除lst.splice(lst.end(), lst2); // 将lst2的所有元素移动到lst末尾lst.sort(); // 排序lst.unique(); // 去重（需相邻元素相同）lst.merge(lst2); // 合并两个已排序的list// 不支持随机访问// lst[0]; // 错误！// 遍历for (auto it = lst.begin(); it != lst.end(); ++it) &#123; // ...&#125; set 有序集合，元素唯一，基于红黑树实现。 插入、删除、查找均为 O (log n) 时间复杂度。 元素自动按升序排列（可自定义比较函数）。(从小到大) 123456789101112131415161718192021std::set&lt;int&gt; s = &#123;3, 1, 2&#125;; // 自动排序为 &#123;1, 2, 3&#125;// 查找s.find(2); // 返回迭代器，若不存在则返回s.end()s.count(2); // 返回元素个数（0或1）s.lower_bound(2); // 返回第一个&gt;=2的元素迭代器s.upper_bound(2); // 返回第一个&gt;2的元素迭代器// 修改s.insert(4); // 插入元素s.erase(2); // 删除元素s.clear();//清空for (auto it = s.begin(); it != s.end(); ++it) &#123; cout &lt;&lt; *it &lt;&lt; &quot; &quot;; &#125;//利用迭代器遍历auto it = s.find(4);it = s.erase(it); // erase返回下一个迭代器，直接赋值给it map 有序键值对，键唯一，基于红黑树实现。 插入、删除、查找、单点访问均为 O (log n) 时间复杂度。 元素按键升序排列。 123456789101112131415161718192021222324252627282930313233map&lt;string, int&gt; m = &#123;&#123;&quot;one&quot;, 1&#125;, &#123;&quot;two&quot;, 2&#125;&#125;;//声明 map 时指定比较器为 greater&lt;int&gt;（int 是键的类型）//从而从大到小排序map&lt;int, string, greater&lt;int&gt;&gt; myMap;// 访问m[&quot;one&quot;]; // 通过键访问值（若键不存在则创建）m.at(&quot;one&quot;); // 通过键访问值（若键不存在则抛出异常）// 查找m.find(&quot;one&quot;); // 返回迭代器m.count(&quot;one&quot;); // 返回键的个数（0或1、这里）// 修改m.insert(&#123;&quot;three&quot;, 3&#125;); // 插入键值对m[&quot;four&quot;] = 4; // 插入或更新m.erase(&quot;one&quot;); // 删除键值对//更为常见的遍历方法for(auto &amp;[x,y]:m)&#123; cout&lt;&lt;x&lt;&lt;&quot; &quot;&lt;&lt;y&lt;&lt;&quot;\\n&quot;;&#125; // 正向遍历for (map&lt;int, string&gt;::iterator it = myMap.begin(); it != myMap.end(); ++it) &#123; cout &lt;&lt; &quot;key: &quot; &lt;&lt; it-&gt;first &lt;&lt; &quot;, value: &quot; &lt;&lt; it-&gt;second &lt;&lt; endl;&#125;// 反向遍历for (map&lt;int, string&gt;::reverse_iterator rit = myMap.rbegin(); rit != myMap.rend(); ++rit) &#123; cout &lt;&lt; &quot;key: &quot; &lt;&lt; rit-&gt;first &lt;&lt; &quot;, value: &quot; &lt;&lt; rit-&gt;second &lt;&lt; endl;&#125; multiset multimap 允许重复元素或键，其他特性与set和map相同 12345678std::multiset&lt;int&gt; ms = &#123;1, 2, 2, 3&#125;;ms.count(2); // 返回2std::multimap&lt;std::string, int&gt; mm = &#123;&#123;&quot;one&quot;, 1&#125;, &#123;&quot;one&quot;, 10&#125;&#125;;auto range = mm.equal_range(&quot;one&quot;); // 返回键为&quot;one&quot;的元素范围for (auto it = range.first; it != range.second; ++it) &#123; // ...&#125; unordered_set 无序集合，元素唯一，基于哈希表实现。 平均 O (1) 时间复杂度的插入、删除、查找。 内存占用较大，且可能需要重新哈希（rehash）。 1234567891011std::unordered_set&lt;int&gt; us = &#123;3, 1, 2&#125;;// 基本与set相同，但不支持lower_bound/upper_boundus.find(2);us.count(2);us.insert(4);us.erase(2);// 哈希表特有的函数us.bucket_count(); // 返回桶的数量us.load_factor(); // 返回负载因子 unordered_map 无序键值对，键唯一，基于哈希表实现。（不建议使用 极大可能被特定数据卡掉） 平均 O (1) 时间复杂度的插入、删除、查找。 12345678std::unordered_map&lt;std::string, int&gt; um = &#123;&#123;&quot;one&quot;, 1&#125;, &#123;&quot;two&quot;, 2&#125;&#125;;// 基本与map相同，但不支持lower_bound/upper_boundum[&quot;one&quot;];um.find(&quot;one&quot;);um.count(&quot;one&quot;);um.insert(&#123;&quot;three&quot;, 3&#125;);um.erase(&quot;one&quot;); stack 后进先出（LIFO）栈，默认基于deque实现。 只允许在栈顶操作。 1234567std::stack&lt;int&gt; st;st.push(1); // 入栈st.top(); // 返回栈顶元素st.pop(); // 出栈st.empty(); // 判断是否为空st.size(); // 返回元素个数 queue 先进先出（FIFO）队列，默认基于deque实现。 支持队首出队、队尾入队。 没有q.clear() 1234567891011std::queue&lt;int&gt; q;q.push(1); // 入队q.front(); // 返回队首元素q.back(); // 返回队尾元素q.pop(); // 出队q.empty(); // 判断是否为空q.size(); // 返回元素个数q=queue&lt;int&gt;();//临时对象替换（用于清空queue） priority_queue 优先队列，最大 &#x2F; 最小元素优先出队，默认基于vector和堆实现。 默认按元素值降序排列（大的在队首）。 无法直接查找元素 1234567891011// 大顶堆（默认）std::priority_queue&lt;int&gt; pq;// 小顶堆std::priority_queue&lt;int, std::vector&lt;int&gt;, std::greater&lt;int&gt;&gt; min_pq;pq.push(3); // 入队pq.top(); // 返回队首元素（最大值）pq.pop(); // 出队pq.empty(); // 判断是否为空pq.size(); // 返回元素个数 所有容器都支持以下操作： begin(), end()：返回指向首尾元素的迭代器。 rbegin(), rend()：返回指向首尾元素的反向迭代器。 empty(), size()：判断容器是否为空、返回元素个数。 clear()：清空容器 其他关于stl容器你可能需要的lower_bound函数时间复杂度logn。在有序区间内，查找第一个大于等于（≥）目标值的元素，返回指向该元素的迭代器；若未找到，则返回区间的尾迭代器（常见于 vector、array、set、map（对键的查找） 等） 1234vector&lt;int&gt; vec = &#123;1, 3, 4, 6, 8, 10&#125;; // 必须是升序有序！ int target = 5; // 查找第一个 ≥ 5 的元素 auto it = lower_bound(vec.begin(), vec.end(), target); upper_bound函数和楼上类似，但是是查找第一个大于的元素 12auto upper = std::upper_bound(vec.begin(), vec.end(), target);std::cout &lt;&lt; (upper - vec.begin()) &lt;&lt; std::endl; 结构体定义比较函数定义后可直接使用sort、set、map等。 123456789struct Node&#123; int l,r; bool operator&lt;(const Node other)const&#123; if(this-&gt;l==other.l)&#123; return this-&gt;r&lt;other.r; &#125; return this-&gt;l&lt;other.l; &#125;//小的在前 pair1. 定义与初始化1234567891011121314#include &lt;utility&gt; // 必须包含此头文件#include &lt;string&gt;// 方式1：默认构造std::pair&lt;int, std::string&gt; p1; // 默认初始化为 &#123;0, &quot;&quot;&#125;// 方式2：直接赋值std::pair&lt;int, std::string&gt; p2(1, &quot;hello&quot;);// 方式3：使用 make_pair() 函数（自动推导类型）auto p3 = std::make_pair(2, &quot;world&quot;);// 方式4：C++11 列表初始化std::pair&lt;int, std::string&gt; p4&#123;3, &quot;cpp&quot;&#125;; 2. 访问元素1234567// 通过 first 和 second 成员访问int num = p2.first; // 获取第一个元素（1）std::string str = p2.second; // 获取第二个元素（&quot;hello&quot;）// 修改元素p2.first = 10;p2.second = &quot;hi&quot;; 3. 比较操作pair 支持比较运算符（&lt;, &gt;, == 等），按字典序比较： 123456std::pair&lt;int, char&gt; p1(1, &#x27;a&#x27;);std::pair&lt;int, char&gt; p2(2, &#x27;b&#x27;);std::pair&lt;int, char&gt; p3(1, &#x27;c&#x27;);p1 &lt; p2; // true（先比较 first，1 &lt; 2）p1 &lt; p3; // true（first 相同，再比较 second，&#x27;a&#x27; &lt; &#x27;c&#x27;） 4.自定义排序12345// 按 second 降序排序std::sort(students.begin(), students.end(), [](const auto&amp; a, const auto&amp; b) &#123; return a.second &gt; b.second; // 年龄从大到小 &#125;); string123456std::string s1; // 默认空字符串std::string s2 = &quot;Hello&quot;; // 直接初始化std::string s3(&quot;World&quot;); // 构造函数初始化std::string s4 = s2 + &quot; &quot; + s3; // 拼接初始化std::string s5(5, &#x27;a&#x27;); // 重复字符：&quot;aaaaa&quot;//数字转字母：char c=&#x27;a&#x27;+2 12s.length(); // 或 s.size()，返回字符串长度s.empty(); // 判断是否为空 12s[0]; // 下标访问（不检查越界）s.at(0); // 安全访问（越界时抛出异常） 123s += &quot;!&quot;; // 追加字符/字符串s.append(&quot; World&quot;); // 追加字符串s.push_back(&#x27;!&#x27;); // 追加单个字符 123s1 == s2; // 内容相等比较s1 != s2; // 内容不等比较s1 &lt; s2; // 字典序比较 1s.substr(pos, len); // 从 pos 开始的 len 个字符 12345s.find(&quot;ll&quot;); // 查找子串，返回首次出现的位置（未找到返回 npos）s.rfind(&quot;l&quot;); // 从后往前查找s.find_first_of(&quot;aeiou&quot;); // 查找首次出现元音的位置//find返回的值是数字或string::npos 123s.insert(pos, &quot;text&quot;); // 在 pos 处插入字符串s.erase(pos, len); // 删除从 pos 开始的 len 个字符s.clear(); // 清空字符串 1s.replace(pos, len, &quot;new&quot;); // 替换从 pos 开始的 len 个字符为 &quot;new&quot; 12const char* c_str = s.c_str(); // 转为 const char*std::string s = std::string(c_str); // 从 const char* 构造 123#include &lt;algorithm&gt;std::transform(s.begin(), s.end(), s.begin(), ::toupper); // 转大写std::transform(s.begin(), s.end(), s.begin(), ::tolower); // 转小写 1234567// string → 数值int num = std::stoi(s); // 转为 intdouble d = std::stod(s); // 转为 double// 数值 → stringstd::string s = std::to_string(42); // 整数转字符串//浮点数（默认保留六位小数）、负数也能转 12for (char c : s) &#123; ... &#125; // 范围-based for 循环for (size_t i = 0; i &lt; s.length(); i++) &#123; ... &#125; // 下标循环 1234567891011#include&lt;bits/stdc++.h&gt;#define int long longusing namespace std;#define IO ios::sync_with_stdio(false);cin.tie(0);typedef long long ll;typedef pair &lt;int , int&gt; pii; 高精加12345678910111213141516171819202122232425262728293031323334353637383940414243#include &lt;iostream&gt;#include &lt;vector&gt;#include &lt;string&gt;#include &lt;algorithm&gt;using namespace std;string add(string a, string b) &#123; // 确保a是较长的字符串 if (a.size() &lt; b.size()) swap(a, b); reverse(a.begin(), a.end()); reverse(b.begin(), b.end()); vector&lt;int&gt; res; int carry = 0; for (int i = 0; i &lt; a.size(); i++) &#123; int digitA = a[i] - &#x27;0&#x27;; int digitB = (i &lt; b.size()) ? b[i] - &#x27;0&#x27; : 0; int sum = digitA + digitB + carry; carry = sum / 10; res.push_back(sum % 10); &#125; if (carry) res.push_back(carry); string ans; for (int i = res.size() - 1; i &gt;= 0; i--) &#123; ans += char(res[i] + &#x27;0&#x27;); &#125; return ans;&#125;int main() &#123; string num1, num2; cout &lt;&lt; &quot;Enter first number: &quot;; cin &gt;&gt; num1; cout &lt;&lt; &quot;Enter second number: &quot;; cin &gt;&gt; num2; string result = add(num1, num2); cout &lt;&lt; &quot;Sum: &quot; &lt;&lt; result &lt;&lt; endl; return 0;&#125; 高精乘12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849#include &lt;iostream&gt;#include &lt;vector&gt;#include &lt;string&gt;#include &lt;algorithm&gt;using namespace std;string multiply(string num1, string num2) &#123; // 处理特殊情况：乘数为0 if (num1 == &quot;0&quot; || num2 == &quot;0&quot;) return &quot;0&quot;; int n = num1.size(), m = num2.size(); // 结果数组，最多有n+m位 vector&lt;int&gt; res(n + m, 0); // 从低位开始计算（字符串从后向前遍历） for (int i = n - 1; i &gt;= 0; i--) &#123; for (int j = m - 1; j &gt;= 0; j--) &#123; int mul = (num1[i] - &#x27;0&#x27;) * (num2[j] - &#x27;0&#x27;); int p1 = i + j, p2 = i + j + 1; // p2是低位位置，p1是高位位置 // 累加到结果数组 int sum = mul + res[p2]; res[p2] = sum % 10; // 当前位 res[p1] += sum / 10; // 进位 &#125; &#125; // 将结果数组转换为字符串 string ans; for (int num : res) &#123; // 跳过前导零（但保留最后一位） if (ans.empty() &amp;&amp; num == 0) continue; ans += to_string(num); &#125; return ans.empty() ? &quot;0&quot; : ans; // 处理结果全零的情况&#125;int main() &#123; string num1, num2; cout &lt;&lt; &quot;Enter first number: &quot;; cin &gt;&gt; num1; cout &lt;&lt; &quot;Enter second number: &quot;; cin &gt;&gt; num2; string result = multiply(num1, num2); cout &lt;&lt; &quot;Product: &quot; &lt;&lt; result &lt;&lt; endl; return 0;&#125; 随机数123456789101112131415#include &lt;iostream&gt;#include &lt;chrono&gt;#include &lt;random&gt;using namespace std;int main()&#123;//unsigned seed =std::chrono::system_clock::now().time_since_epoch().count();//mt19937 rand_num(seed); 方法一random_device divc;mt19937 rand_num(divc()); //方法二：Linux真随机数uniform_int_distribution&lt;long long&gt; dist(0, 1000000000);cout &lt;&lt; dist(rand_num) &lt;&lt; endl;return 0;&#125; 迪杰斯特拉算法12345678910111213141516171819202122232425262728293031323334353637383940#include&lt;bits/stdc++.h&gt;using namespace std;typedef long long int ll;int n,m,s;vector&lt;pair&lt;int,ll&gt;&gt; t[100002];vector&lt;ll&gt; dis(100002,1e9+1);bool vis[100002];int main()&#123; cin&gt;&gt;n&gt;&gt;m&gt;&gt;s; int i; ll u,v,w; for(i=0;i&lt;m;i++)&#123; cin&gt;&gt;u&gt;&gt;v&gt;&gt;w; t[u].push_back(&#123;v,w&#125;); &#125; priority_queue&lt;pair&lt;ll,int&gt;,vector&lt;pair&lt;ll,int&gt;&gt;,greater&lt;pair&lt;ll,int&gt;&gt;&gt; dui; dui.push(&#123;0,1&#125;); dis[1]=0; while(!dui.empty())&#123; int v=dui.top().second; dui.pop(); if(vis[v])&#123; continue; &#125; vis[v]=true; for(auto [u,len] :t[v])&#123; if(dis[u]&gt;dis[v]+len)&#123; dis[u]=dis[v]+len; dui.push(&#123;dis[u],u&#125;); &#125; &#125; &#125; for(i=1;i&lt;=n;i++)&#123; cout&lt;&lt;dis[i]&lt;&lt;&quot; &quot;; &#125; return 0;&#125; 加速1234567891011121314151617181920212223242526//手动o2、o3、ofast优化#pragma GCC optimize(2)#pragma GCC optimize(3)#pragma GCC optimize(&quot;Ofast,unroll-loops&quot;)template&lt;typename type&gt;//快读inline void read(type &amp;x)&#123; x=0;static bool flag(0);char ch=getchar(); while(!isdigit(ch)) flag=ch==&#x27;-&#x27;,ch=getchar(); while(isdigit(ch)) x=(x&lt;&lt;1)+(x&lt;&lt;3)+(ch^48),ch=getchar(); flag?x=-x:0;&#125;template&lt;typename type&gt;//快写inline void write(type x,bool mode=1)//0为空格，1为换行&#123; x&lt;0?x=-x,putchar(&#x27;-&#x27;):0;static short Stack[50],top(0); do Stack[++top]=x%10,x/=10; while(x); while(top) putchar(Stack[top--]|48); mode?putchar(&#x27;\\n&#x27;):putchar(&#x27; &#x27;);&#125;#define IO ios::sync_with_stdio(false);cin.tie(0); 输入输出12345678double ans;cout&lt;&lt;fixed&lt;&lt;setprecision(10)&lt;&lt;ans&lt;&lt;&quot;\\n&quot;;//保留十位小数输出string s = &quot;abc&quot;; cout &lt;&lt; setw(6) &lt;&lt; setfill(&#x27;*&#x27;) &lt;&lt; s &lt;&lt; endl;// 输出 固定宽度 + 前导0 // cout &lt;&lt; setw(宽度) &lt;&lt; setfill(&#x27;0&#x27;) &lt;&lt; 变量; 1234567891011int num;string text;cout &lt;&lt; &quot;输入数字: &quot;;cin &gt;&gt; num;cin.ignore(); // 清除输入缓冲区的换行符cout &lt;&lt; &quot;输入带空格的字符串: &quot;;getline(cin, text); // 正常读取//使用getline读取一行，getline间不需要ignore，cin+getline才需 二维数组排序1. 按行排序（每行内部元素排序）若需对二维数组的每一行单独排序（如将每行元素按升序排列），可遍历每行并使用std::sort： 123456789101112#include &lt;algorithm&gt;int arr[3][4] = &#123; &#123;5, 3, 8, 1&#125;, &#123;4, 9, 2, 7&#125;, &#123;6, 0, 3, 5&#125;&#125;;// 对每一行排序for (int i = 0; i &lt; 3; ++i) &#123; std::sort(arr[i], arr[i] + 4); // 对第i行的4个元素排序&#125; 结果： 1231 3 5 82 4 7 90 3 5 6 2. 按列排序（每列内部元素排序）若需对二维数组的每一列单独排序（如将每列元素按升序排列），需逐列遍历并排序： 12345678910111213141516171819#include &lt;algorithm&gt;int arr[3][4] = &#123; &#123;5, 3, 8, 1&#125;, &#123;4, 9, 2, 7&#125;, &#123;6, 0, 3, 5&#125;&#125;;// 对每一列排序for (int j = 0; j &lt; 4; ++j) &#123; std::vector&lt;int&gt; column; for (int i = 0; i &lt; 3; ++i) &#123; column.push_back(arr[i][j]); &#125; std::sort(column.begin(), column.end()); for (int i = 0; i &lt; 3; ++i) &#123; arr[i][j] = column[i]; &#125;&#125; 结果： 1234 0 2 15 3 3 56 9 8 7 3. 按特定列排序（整行移动）若需根据某一列的值对所有行进行排序（类似 Excel 按列排序），可将每行转换为vector或自定义结构体，再通过std::sort的比较函数实现： 123456789101112131415161718192021222324252627282930313233#include &lt;algorithm&gt;#include &lt;vector&gt;struct Row &#123; int data[4];&#125;;bool compareByColumn(const Row&amp; a, const Row&amp; b, int col) &#123; return a.data[col] &lt; b.data[col];&#125;int main() &#123; std::vector&lt;Row&gt; rows = &#123; &#123;5, 3, 8, 1&#125;, &#123;4, 9, 2, 7&#125;, &#123;6, 0, 3, 5&#125; &#125;; // 按第2列（索引1）排序 std::sort(rows.begin(), rows.end(), [](const Row&amp; a, const Row&amp; b) &#123; return a.data[1] &lt; b.data[1]; &#125;); // 输出排序后的结果 for (const auto&amp; row : rows) &#123; for (int i = 0; i &lt; 4; ++i) &#123; std::cout &lt;&lt; row.data[i] &lt;&lt; &quot; &quot;; &#125; std::cout &lt;&lt; &quot;\\n&quot;; &#125; return 0;&#125; 结果（按第 2 列排序）： 1236 0 3 55 3 8 14 9 2 7 4. 自定义多维排序规则若需按多列组合排序（如先按第 1 列升序，再按第 2 列降序），可在比较函数中定义复杂规则： 1234567std::sort(rows.begin(), rows.end(), [](const Row&amp; a, const Row&amp; b) &#123; if (a.data[0] != b.data[0]) &#123; return a.data[0] &lt; b.data[0]; // 第1列升序 &#125; return a.data[1] &gt; b.data[1]; // 第2列降序（若第1列相同） &#125;); 最大公因数123456789101112#include &lt;iostream&gt;// 递归实现欧几里得算法int gcd(int a, int b) &#123; return b == 0 ? a : gcd(b, a % b);&#125;int main() &#123; int a = 24, b = 36; std::cout &lt;&lt; &quot;GCD of &quot; &lt;&lt; a &lt;&lt; &quot; and &quot; &lt;&lt; b &lt;&lt; &quot; is: &quot; &lt;&lt; gcd(a, b) &lt;&lt; std::endl; return 0;&#125; 二进制相关0.呃，新加的 2的30次方大约是1e9+ 2的20次方是1e6+ 2的50次方是1e15+ 2的60次方是1e18+ 0.5.呃，我不知道 int 范围 -2^31 ~ 2^31-1 long long 范围 -2^63 ~ 2^63-1 unsigned long long 范围 0 ~ 2^64-1 int a&#x3D;1&lt;&lt;31（错误 会爆int，因为1是int）int a&#x3D;1LL&lt;&lt;31（正确）&#x2F;&#x2F;呃 我有dill 组合数运算中的 \\binom{n}{n&#x2F;2}，n&gt;32 会爆int，n&gt;66 会爆longlong 1. 基本运算符 按位与（&amp;）：两对应位都为 1 时结果为 1，常用于清零或取特定位。例：5 &amp; 3 = 1（二进制：101 &amp; 011 = 001）。 按位或（|）：两对应位有一个为 1 时结果为 1，常用于置位。例：5 | 3 = 7（二进制：101 | 011 = 111）。 按位异或（^）：两对应位不同时结果为 1，常用于取反或交换。例：5 ^ 3 = 6（二进制：101 ^ 011 = 110）。 按位取反（~）：对每一位取反，常用于生成掩码。例：~5 = -6（二进制：~0101 = 1010，补码表示为 - 6）。 左移（&lt;&lt;）：将二进制位左移，低位补 0，等价于乘以 2 的幂。例：5 &lt;&lt; 1 = 10（二进制：101 → 1010）。 右移（&gt;&gt;）：将二进制位右移，高位补符号位（算术右移）或 0（逻辑右移），等价于除以 2 的幂。例：5 &gt;&gt; 1 = 2（二进制：101 → 10）。 2. 常见用途 判断奇偶：n &amp; 1 为 1 则奇数，为 0 则偶数。 交换两数：a ^= b; b ^= a; a ^= b;。 取绝对值（补码系统）：(n ^ (n &gt;&gt; 31)) - (n &gt;&gt; 31)。 3. 一些结论 x+y=(x∣y)+(x&amp;y)x^y=(x∣y)−(x&amp;y) x^y=z 那么 z^y=x``z^x=y 如果 x^y=gcd(x,y)=c 那么 x-y=c (x&gt;&#x3D;y) 1. 快速幂（Exponentiation by Squaring）计算 an 的时间复杂度为 O(logn)： 123456789int q_p(int a, int n) &#123; int res = 1; while (n &gt; 0) &#123; if (n &amp; 1) res *= a; // 若当前位为1，则乘入结果 a *= a; // 底数平方 n &gt;&gt;= 1; // 右移一位 &#125; return res;&#125; 2. 快速乘法计算 a×bmodm，避免大数溢出： 123456789long long fast_mul(long long a, long long b, long long m) &#123; long long res = 0; while (b &gt; 0) &#123; if (b &amp; 1) res = (res + a) % m; a = (a * 2) % m; b &gt;&gt;= 1; &#125; return res;&#125; 异或线性基异或线性基的性质： 原序列里面的任意一个数都可以通过异或线性基中的一些数异或得到 异或线性基里面的任意一些数异或起来都不能是得到了0 。 线性基里面的数的个数唯一，并且在保持性质 1 的前提下，数的个数是最少的。 小tips：线性基的每一位维护的都是二进制的最高位是这一位的数 构造方法：123456789101112131415void add(int x) &#123; for (int i = 60; i &gt;= 0; i--) &#123; if ((x &amp; (1ll &lt;&lt; i)) == 0) continue; //如果该位没有 1，则不会成为对应线性基 else if (basis[i] &gt; 0) x ^= basis[i]; //如果该位有基，那么去掉 x 对应的 1 else &#123; basis[i] = x; break; &#125; //如果该位没有基，那么 x 成为基 &#125;&#125;//在原序列中加入一个新的元素，并考虑是否会有新的基 运用 判断x能否被集合异或表示 得到子集异或最大值 组合数阶乘逆元法预处理阶乘，套公式用费马小定理，注意模数必须是质数 \\binom{n}{k} &#x3D; \\frac{n!}{k!*(n-k)!} 递推&#x2F;dp法（杨辉三角法）一个高中学的小式子： \\binom{n}{k}&#x3D;\\binom{n-1}{k-1}+\\binom{n-1}{k} 12345678910 for(int i=0;i&lt;=N;i++)&#123;for(int j=0;j&lt;=i;j++)&#123; if(j==0)&#123; cc[i][j]=1; &#125; else&#123; cc[i][j]=(cc[i-1][j]+cc[i-1][j-1])%Mod; &#125;&#125; &#125; 可能是数学 内置sin cos tan asin acos atan，接受一个参数（弧度制），返回一个浮点数。 1234#include&lt;cmath&gt;cout&lt;&lt;sin(90/180*3.1415926);//输出1 无法避免需要一定精度的开根的话，可以使用sqrt（绝对误差通常在1e-15级别） 1234#include &lt;cmath&gt;double precise_sqrt(double x) &#123; return std::sqrt(x); // double 精度足够覆盖 1e-9 要求&#125; 上下取整可以直接用函数 12345678//上取整 ceil double num = 3.14; // 向上取整后转 int int result = (int)ceil(num); //result=4 //负数上取整 -2.x -&gt; -2//下取整 int res = (int)floor(3.9); //负数下取整 -2.x -&gt; -3 模运算加、减、乘 正常mod，like this： (a+b)%mod &#x3D;&#x3D; ( a%mod+b%mod )%mod 除法需用费马小定理( Mod 必须是质数 )： a&#x2F;b 的 mod 就是（a qpow（b，mod-2））%mod 欧拉筛（质数）12345678910111213141516171819202122232425bool isprime[MAXN]; // isprime[i]表示i是不是素数int prime[MAXN]; // 现在已经筛出的素数列表int n; // 上限，即筛出&lt;=n的素数int cnt; // 已经筛出的素数个数void euler()&#123; memset(isprime, true, sizeof(isprime)); //先全部标记为素数 isprime[1] = false; // 1不是素数 for(int i = 2; i &lt;= n; ++i) // i从2循环到n（外层循环） &#123; if(isprime[i]) prime[++cnt] = i; // 如果i没有被前面的数筛掉，则i是素数 for(int j = 1; j &lt;= cnt &amp;&amp; i * prime[j] &lt;= n; ++j) // 筛掉i的素数倍，即i的prime[j]倍 // j循环枚举现在已经筛出的素数（内层循环） &#123; isprime[i * prime[j]] = false; // 倍数标记为合数，也就是i用prime[j]把i * prime[j]筛掉了 if(i % prime[j] == 0) break; // 最神奇的一句话，如果i整除prime[j]，退出循环 // 这样可以保证线性的时间复杂度 &#125; &#125;&#125; 埃氏筛从最小的质数（2）开始，依次将每个质数的倍数标记为非质数。对于每个质数 p，需要标记的倍数是 2p, 3p, ..., ⌊n/p⌋·p，共 ⌊n/p⌋ - 1 个数。 其时间复杂度的推导基于调和级数的近似： 筛选过程中，对每个质数 p 的操作次数约为 n/p。 所有质数的 1/p 之和近似为 log log n（这是数论中的一个经典结论）。 因此，总操作次数约为 n log log n。 123456789101112131415161718vector&lt;bool&gt; is_prime(n + 1, true);is_prime[0] = is_prime[1] = false;for (int p = 2; p * p &lt;= n; ++p) &#123; if (is_prime[p]) &#123; for (int i = p * p; i &lt;= n; i += p) &#123; // 从 p*p 开始标记（优化） is_prime[i] = false; &#125; &#125;&#125;//pfac 是一个存储每个数的质因数（不重复）的二维向量，埃氏筛求质因数for (int i = 2; i &lt;= N; i++)&#123; if (!pfac[i].empty()) continue; for (int j = i; j &lt;= N; j += i) pfac[j].push_back(i); &#125; 判断是否为质数1234567891011bool isPrime(int x) &#123; if(x&lt;=1)return false; if(x&lt;=3)return true; if(x%2==0||x%3==0)return false; for(int i=5;i*i&lt;=x;i+=6)&#123; if(x%i==0||x%(i+2)==0)&#123; return false; &#125; &#125; return true;&#125; 并查集解决元素分组的问题，用于管理一系列不相交的集合。支持的操作： 合并（Union）：把两个不相交的集合合并为一个集合。 查询（Find）：查询两个元素是否在同一个集合中。 上代码 1234567891011121314151617181920212223242526272829inline void init(int n) //初始化，（rank代表秩，是一种优化）&#123; for (int i = 1; i &lt;= n; ++i) &#123; fa[i] = i; rank[i] = 1; &#125;&#125;int find(int x) //路径压缩&#123; return x == fa[x] ? x : (fa[x] = find(fa[x]));&#125;inline void merge(int i, int j) //按秩合并&#123; int x = find(i), y = find(j); if (rank[x] &lt;= rank[y]) fa[x] = y; else fa[y] = x; if (rank[x] == rank[y] &amp;&amp; x != y) rank[y]++;&#125;inline void merge(int i, int j) //普通合并&#123; fa[find(i)] = find(j);&#125; 树相关递归求树深（树高） 12345678910int dep(int v)&#123; if(son[v].empty())&#123; return 1; &#125; int res=0; for(auto u:son[v])&#123; res=max(res,dep(u)); &#125; return res+1;&#125; 无向图最小生成树prim算法稠密图适用。 1234567891011121314151617181920212223242526272829303132333435int n;int a[N][N]; //完全图vector&lt;int&gt; G[N]; //最后生成的无根树int dis[N],vis[N],pre[N];void prim()&#123; for(int i=1;i&lt;=n;i++)&#123; dis[i]=inf; vis[i]=0; pre[i]=0; &#125; for(int i=1;i&lt;=n;i++)&#123; int x=0; for(int j=1;j&lt;=n;j++)&#123; // cout&lt;&lt;dis[j]&lt;&lt;&quot; &quot;; if(!vis[j]&amp;&amp;(x==0||dis[j]&lt;dis[x]))&#123; x=j; &#125; &#125; // cout&lt;&lt;&quot;&lt;dis&gt;\\n&quot;; for(int j=1;j&lt;=n;j++)&#123; if(j==x)continue; if(dis[j]&gt;a[x][j])&#123; dis[j]=a[x][j]; pre[j]=x; &#125; // cout&lt;&lt;x&lt;&lt;&quot; &quot;&lt;&lt;j&lt;&lt;&quot; &quot;&lt;&lt;dis[j]&lt;&lt;&quot; &quot;&lt;&lt;a[x][j]&lt;&lt;&quot;&lt;bian&gt;\\n&quot;; &#125; if(pre[x]!=0)&#123; G[pre[x]].push_back(x); G[x].push_back(pre[x]); &#125; vis[x]=1; &#125;&#125; Kruskal稀疏图适用（贪心+并查集） 12345678910111213141516171819202122232425262728293031323334353637383940414243444546474849505152#include&lt;iostream&gt;#include&lt;cstdio&gt;#include&lt;algorithm&gt;using namespace std;int n,cnt,num,ans,m;//n为点的数量，m是边的总数，num是已经选取了的边数 int fa[105];//存储每个点的父亲 struct Edge&#123; int w;//边权 int to,pre;//边相连的两个点 &#125;;Edge len[1000];//结构体存储更方便调用 bool cmp(Edge x,Edge y)//使sort按照边权大小排序 &#123; return x.w &lt; y.w;&#125;int find(int x)//并查集 找父亲的函数//注意这里使用的是递归查找，可以让这个点（A）的父亲也认A的祖先做父亲，使得一次递归经过的所有点的父亲都变成祖先，大大降低了时间复杂度。 &#123; if(fa[x]==x) return x; return fa[x]=find(fa[x]);&#125;void build(int x)//选取函数，答案加上边权值，然后两个点相连 &#123; ans+=len[x].w; fa[find(len[x].pre)]=find(len[x].to); return;&#125;int main()&#123; int x,y,we;//两个点和边权 cin&gt;&gt;n&gt;&gt;m;//n个点 m个边 for(int i=1;i&lt;=m;i++) &#123; cin&gt;&gt;x&gt;&gt;y&gt;&gt;we; len[i].to=x; len[i].pre=y; len[i].w=we; &#125; sort(len+1,len+m+1,cmp);//按边权排序 for(int i=1;i&lt;=n;i++) fa[i]=i;//重点！必须事先把每个点的父亲赋值为它自己，不然默认每个点父亲都是0，会出错。 for(int i=1;i&lt;=m;i++)//按边权从小到大枚举 &#123; if(num==n-1) break;//如果选取的边足够了，就停止循环 if(find(len[i].pre)==find(len[i].to)) continue;//如果两个点的父亲相同（已经合并了），就不管它了 else&#123; build(i); num++;//已经选取的边数+1 &#125; &#125; printf(&quot;%d&quot;,ans); return 0;&#125; 拓扑排序在图论中，拓扑排序（Topological Sorting）是一个有向无环图（DAG, Directed Acyclic Graph）的所有顶点的线性序列。且该序列必须满足下面两个条件： 12每个顶点出现且只出现一次。若存在一条从顶点 A 到顶点 B 的路径，那么在序列中顶点 A 出现在顶点 B 的前面。 有向无环图（DAG）才有拓扑排序，非DAG图没有拓扑排序一说。 12345678910111213141516171819202122232425for(auto x: edge) &#123; int fi=x.first, se=x.second; e[fi].push_back(se); //邻接表存储 pre[se]++; //统计入度 &#125;queue&lt;int&gt;q = queue&lt;int&gt;();vector&lt;int&gt;order = vector&lt;int&gt;();for(int i=1;i&lt;=n;i++) &#123; if(pre[i]==0) &#123; q.push(i); &#125;&#125;while(!q.empty()) &#123; int x=q.front(); q.pop(); order.push_back(x); for(auto y: e[x]) &#123; pre[y]--; //x已经被排序了，因此x的后继节点入度应该-1 if(pre==0) &#123; q.push(y); &#125; &#125;&#125;","updated":"2026-09-07T06:51:20.182Z","excerpt":"","categories":[{"name":"learn","slug":"learn","permalink":"http://example.com/categories/learn/"}],"tags":[{"name":"Algorithm","slug":"Algorithm","permalink":"http://example.com/tags/Algorithm/"}]}]